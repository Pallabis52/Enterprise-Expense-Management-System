import React from "react";
import { Container, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Container>
      <Typography variant="h4">Welcome to Expense Manager</Typography>
    </Container>
  );
};

export default Dashboard;